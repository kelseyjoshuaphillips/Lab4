@model IEnumerable<Lab4.Models.ViewModels.NewsBoardViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NewsBoards.First().Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NewsBoards.First().Fee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NewsBoards.First().Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NewsBoards.First().Fee)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NewsBoards.First().Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.NewsBoards.First().Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.NewsBoards.First().Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
